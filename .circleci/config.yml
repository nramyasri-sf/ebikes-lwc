jobs:
  install_authenticate:
    executor: sfdx/default
    steps:
      - checkout
      - sfdx/install
      - sfdx/auth:
          defaultusername: rn@su-blitz.org
      - run:
          command: >
            sfdx force:org:create -f ./config/project-scratch-def.json --alias=testOrg
          name: List Auth files
      - run:
         command: >
           sfdx force:source:push -u testOrg    
orbs:
  sfdx: circleci/salesforce-sfdx@1.0.0
version: 2.1
workflows:
  basic-test:
    jobs:
      - install_authenticate